python3 -m venv /media/bigdisk/ws_venv
cd /media/bigdisk/ws_venv
source ./bin/activate

mkdir git
cd git
git clone https://github.com/stts-se/wikispeech_mockup.git
cd wikispeech_mockup
git checkout packaging
mkdir wikispeech_mockup/tmp

#should fail with "ImportError: No module named 'requests'":
bin/wikispeech

python3 setup.py install
#Need to run it twice, fails on flask the first time, WHY?
python3 setup.py install

#Now works:
python3 wikispeech_mockup/wikispeech.py

deactivate
